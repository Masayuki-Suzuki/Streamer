import React from 'react'

type StreamCreatePropsType = unknown

const StreamCreate = (props: StreamCreatePropsType): JSX.Element => {
    return <div>StreamCreate</div>
}

export default StreamCreate
