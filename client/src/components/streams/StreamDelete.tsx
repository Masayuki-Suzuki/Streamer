import React from 'react'

type StreamDeletePropsType = unknown

const StreamDelete = (props: StreamDeletePropsType): JSX.Element => {
    return <div>StreamDelete</div>
}

export default StreamDelete
